<UserControl x:Class="Timesheets.WinClient.RootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Timesheets.WinClient"
             mc:Ignorable="d" 
             Width="400"
             Height="500">
    <Grid>
        <StackPanel>
            <ListBox ItemsSource="{Binding Timesheets}" SelectedValue="{Binding SelectedTimesheet}" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>FooBar</TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Name="ViewOrAmendTimesheet" Margin="5">Amend</Button>
            <Button Name="CreateTimesheet" Margin="5">New</Button>
        </StackPanel>            
    </Grid>
</UserControl>
